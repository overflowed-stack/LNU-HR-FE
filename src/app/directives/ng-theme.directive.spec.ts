import { NgThemeStatusDirective } from './ng-theme.directive';

describe('NgThemeStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new NgThemeStatusDirective();
    expect(directive).toBeTruthy();
  });
});
